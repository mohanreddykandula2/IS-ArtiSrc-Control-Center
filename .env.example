# No environment variables are required for local ZIP mode.
#
# SAP CPI credentials are entered in the application only when using
# "Connect to CPI" mode. Client secrets are not stored in persistent
# browser local storage.
